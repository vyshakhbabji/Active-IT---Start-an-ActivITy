/*
 * JS for SignIn generated by Appery.io
 *
 */

Appery.getProjectGUID = function() {
    return 'd622a8af-a8d6-4ae9-a795-f5cb14b11caf';
}

function navigateTo(outcome, useAjax) {
    Appery.navigateTo(outcome, useAjax);
}

// Deprecated


function adjustContentHeight() {
    Appery.adjustContentHeightWithPadding();
}

function adjustContentHeightWithPadding(_page) {
    Appery.adjustContentHeightWithPadding(_page);
}

function setDetailContent(pageUrl) {
    Appery.setDetailContent(pageUrl);
}

// Appery push registration service
var _pushRegistrationApperyService = new Appery.RestService({
    'url': 'https://api.appery.io/rest/push/reg',
    'dataType': 'json',
    'type': 'post',
    'contentType': 'application/json',
});

Appery.AppPages = [{
    "name": "SignIn",
    "location": "SignIn.html"
}, {
    "name": "FinalResult",
    "location": "FinalResult.html"
}, {
    "name": "startScreen",
    "location": "startScreen.html"
}, {
    "name": "searchPeople",
    "location": "searchPeople.html"
}, {
    "name": "EventsDisplay",
    "location": "EventsDisplay.html"
}];

SignIn_js = function(runBeforeShow) { /* Object & array with components "name-to-id" mapping */
    var n2id_buf = {
        'mobileimage_20': 'SignIn_mobileimage_20',
        'mobilelabel_19': 'SignIn_mobilelabel_19',
        'mobilegrid_1': 'SignIn_mobilegrid_1',
        'mobilegridcell_2': 'SignIn_mobilegridcell_2',
        'userNameLabel': 'SignIn_userNameLabel',
        'mobilegridcell_3': 'SignIn_mobilegridcell_3',
        'userNameTextField': 'SignIn_userNameTextField',
        'mobilegridcell_4': 'SignIn_mobilegridcell_4',
        'passwordLabel': 'SignIn_passwordLabel',
        'mobilegridcell_5': 'SignIn_mobilegridcell_5',
        'passwordTextField': 'SignIn_passwordTextField',
        'mobilegrid_14': 'SignIn_mobilegrid_14',
        'mobilegridcell_15': 'SignIn_mobilegridcell_15',
        'signInButton': 'SignIn_signInButton',
        'mobilegridcell_16': 'SignIn_mobilegridcell_16',
        'signUpButton': 'SignIn_signUpButton',
        'mobilegridcell_17': 'SignIn_mobilegridcell_17',
        'mobilegridcell_18': 'SignIn_mobilegridcell_18'
    };

    if ("n2id" in window && window.n2id !== undefined) {
        $.extend(n2id, n2id_buf);
    } else {
        window.n2id = n2id_buf;
    }

    if (navigator.userAgent.indexOf("IEMobile") != -1) {
        //Fixing issue https://github.com/jquery/jquery-mobile/issues/5424 on Windows Phone
        $("div[data-role=page] div[data-role=footer]").css("bottom", "-36px");
    }

    if (Appery.getTargetPlatform == "I") {
        $.each(window.n2id, function(name, id) {
            var elApperyRole = $("#" + id).attr("data-role");
            if (elApperyRole != "appery_label" && elApperyRole != "appery_link") {
                var appleDataSelector = "#" + id + "[x-apple-data-detectors], #" + id + " [x-apple-data-detectors]";
                $(appleDataSelector).attr("x-apple-data-detectors", "false");
            }
        });
    }

    Appery.CurrentScreen = 'SignIn';

    /*
     * Nonvisual components
     */
    var datasources = [];

    loginservice = new Appery.DataSource(ActiveFriend_login_service, {
        'onComplete': function(jqXHR, textStatus) {

            $t.refreshScreenFormElements("SignIn");
        },
        'onSuccess': function(data) {
            setVar_('username', 'SignIn_userNameTextField', 'text', '', this);
            Appery.navigateTo('searchPeople', {
                transition: 'slide',
                reverse: false
            });
        },
        'onError': function(jqXHR, textStatus, errorThrown) {
            alert('fail');
        },
        'responseMapping': [],
        'requestMapping': [{
            'PATH': ['X-Appery-Database-Id'],
            'TYPE': 'STRING',
            'HEADER': true,
            'ATTR': '{database_id}'
        }, {
            'PATH': ['username'],
            'TYPE': 'STRING',
            'ID': 'userNameTextField',
            'ATTR': 'value'
        }, {
            'PATH': ['password'],
            'TYPE': 'STRING',
            'ID': 'passwordTextField',
            'ATTR': 'value'
        }]
    });

    datasources.push(loginservice);

    /*
     * Appery Push-notification registration service
     */
    var _registerPushApperyDS = new Appery.DataSource(_pushRegistrationApperyService, {
        'onComplete': function(jqXHR, textStatus) {
            $t.refreshScreenFormElements("SignIn");
        },
        'onSuccess': function(data) {
            console.info('App successfully registered with Appery.io Push service');
            $(document).trigger('pushregistrationsuccess');
        },
        'onError': function(jqXHR, textStatus, errorThrown) {
            $(document).trigger('pushregistrationfail');
        },
        'responseMapping': [],
        'requestMapping': [{
            'PATH': ['type'],
            'TRANSFORMATION': function(value) {
                return Appery.getTargetPlatform();
            }
        }, {
            'PATH': ['token'],
            'ID': '___local_storage___',
            'ATTR': 'pushNotificationToken'
        }, {
            'PATH': ['deviceID'],
            'ID': '___local_storage___',
            'ATTR': 'pushNotificationDeviceID'
        }, {
            'PATH': ['X-Appery-App-Id'],
            'HEADER': true,
            'TRANSFORMATION': function(value) {
                return Appery.getProjectGUID();
            }
        }, {
            'PATH': ['timeZone'],
            'ID': '___local_storage___',
            'ATTR': 'deviceTimeZone'
        }]
    });

    /*
     * Events and handlers
     */

    // Before Show
    SignIn_beforeshow = function() {
        Appery.CurrentScreen = "SignIn";
        for (var idx = 0; idx < datasources.length; idx++) {
            datasources[idx].__setupDisplay();
        }
    }

    // On Load
    screen_9C6E_onLoad = SignIn_onLoad = function() {
        screen_9C6E_elementsExtraJS();

        // TODO fire device events only if necessary (with JS logic)
        SignIn_deviceEvents();
        SignIn_windowEvents();
        screen_9C6E_elementsEvents();
    }

    // screen window events
    screen_9C6E_windowEvents = SignIn_windowEvents = function() {

        $('#SignIn').bind('pageshow orientationchange', function() {
            var _page = this;
            adjustContentHeightWithPadding(_page);
        });

    }

    // device events
    SignIn_deviceEvents = function() {

        document.addEventListener("deviceready", function() {

            $(document).bind("resume", function() {
                window.plugins.pushNotification.getPendingNotifications(function(status) {
                    var notifications = status.notifications;
                    for (var i = 0; i < notifications.length; ++i) {
                        window.plugins.pushNotification.notificationCallback(notifications[i]);
                    }
                });
            });
            $(document).bind("pushinit", function() {
                localStorage.setItem('pushNotificationToken', arguments[1].deviceToken);
                _registerPushApperyDS.execute({});
            });

            var pushNotification = window.plugins.pushNotification;
            pushNotification.getDeviceUniqueIdentifier(function(status) {
                localStorage.setItem('pushNotificationDeviceID', status);
            });

            var offset = new Date().getTimezoneOffset();
            var hr = parseInt(offset / (-60));
            var min = -offset - hr * 60;
            var tmin = '' + min;
            var timezone = 'UTC' + (hr > 0 ? '+' + hr : hr) + ':' + (tmin.length > 1 ? tmin : '0' + tmin);
            localStorage.setItem('deviceTimeZone', timezone);

            pushNotification.registerDevice({
                alert: true,
                badge: false,
                sound: true,
                senderid: 'http://appery.io'
            }, function(status) {
                $(document).trigger('pushinit', status);
            });
        });
    }

    // screen elements extra js
    screen_9C6E_elementsExtraJS = SignIn_elementsExtraJS = function() {
        // screen (SignIn) extra code

    }

    // screen elements handler
    screen_9C6E_elementsEvents = SignIn_elementsEvents = function() {

        $("a :input,a a,a fieldset label").live({
            click: function(event) {
                event.stopPropagation();
            }
        });

        $('#SignIn_mobilecontainer [name="signInButton"]').die().live({
            click: function() {
                if (!$(this).attr('disabled')) {
                    try {
                        loginservice.execute({})
                    } catch (ex) {
                        console.log(ex.name + '  ' + ex.message);
                        hideSpinner();
                    };

                }
            },
        });

        $('#SignIn_mobilecontainer [name="signUpButton"]').die().live({
            click: function() {
                if (!$(this).attr('disabled')) {
                    Appery.navigateTo('startScreen', {
                        reverse: false
                    });

                }
            },
        });

    }

    $("#SignIn").die("pagebeforeshow").live("pagebeforeshow", function(event, ui) {
        SignIn_beforeshow();
    });

    if (runBeforeShow) {
        SignIn_beforeshow();
    } else {
        SignIn_onLoad();
    }

}

$("#SignIn").die("pageinit").live("pageinit", function(event, ui) {
    Appery.processSelectMenu($(this));
    SignIn_js();
});